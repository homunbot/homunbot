# Homun — Docker Compose stack
#
# Quick start:
#   1. cp .env.example .env
#   2. Edit .env (set HOMUN_DOMAIN for production HTTPS)
#   3. docker compose up -d
#   4. Open https://your-domain (or https://localhost for dev)
#   5. Complete the setup wizard to create admin account
#
# With local embeddings (free, no API key):
#   docker compose --profile with-ollama up -d

services:
  homun:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: homun
    restart: unless-stopped
    volumes:
      - homun-data:/home/homun/.homun
    env_file:
      - .env
    networks:
      - internal
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:18080/api/health"]
      interval: 30s
      timeout: 5s
      start_period: 30s
      retries: 3

  caddy:
    image: caddy:2-alpine
    container_name: caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    env_file:
      - .env
    networks:
      - internal
    depends_on:
      homun:
        condition: service_healthy

  # Optional: Ollama for free local embeddings (and local LLMs)
  # Activate with: docker compose --profile with-ollama up -d
  ollama:
    image: ollama/ollama
    container_name: ollama
    profiles: ["with-ollama"]
    restart: unless-stopped
    volumes:
      - ollama-data:/root/.ollama
    networks:
      - internal

volumes:
  homun-data:
    name: homun-data
  caddy-data:
    name: caddy-data
  caddy-config:
    name: caddy-config
  ollama-data:
    name: ollama-data

networks:
  internal:
    driver: bridge
