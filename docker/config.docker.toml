# Homun — Docker default configuration
# This file is copied into the image as seed config.
# User config is persisted in the homun-data volume.
# All other settings are configured via the web UI setup wizard.

[channels.web]
host = "0.0.0.0"
port = 18080
auto_tls = false
domain = ""
