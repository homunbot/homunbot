/**
 * Sidebar sub-navigation.
 * Sub-nav visibility is fully server-rendered via .is-open class.
 * No JS needed.
 */
(function () {
    // No JS needed — sub-nav visibility is fully server-rendered.
})();
